 ∇ start
 ⍝ Starts the web server for the UW Schedule Exporter application
 ⍝
 ⍝ This function initializes and starts a Jarvis web server on port 8080
 ⍝ with the following configuration:
 ⍝ - Port: 8080
 ⍝ - Namespace: web (contains request handlers)
 ⍝ - Payload format: JSON
 ⍝ - Default page: Index function in web namespace
 ⍝ - Allows both GET and POST requests
 ⍝
 ⍝ Global variables created:
 ⍝ - JSrv: Jarvis server instance for later control (stop, restart, etc.)
 ⍝
 ⍝ Usage: start
 ⍝ After running, the web interface will be available at http://localhost:8080
      JSrv←Jarvis.New(8080 web'JSON')
      JSrv.DYALOG_JARVIS_THREAD←'0'
      JSrv.HTMLInterface←'' 'Index'
      JSrv.AllowGETs←1
      JSrv.AllowPOSTs←1
      JSrv.Start
 ∇
